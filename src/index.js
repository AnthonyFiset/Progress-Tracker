import React from 'react';
import ReactDOM from 'react-dom';
import ProgressTracker from './App';

ReactDOM.render(
  <React.StrictMode>
    <ProgressTracker />
  </React.StrictMode>,
  document.getElementById('root')
);
